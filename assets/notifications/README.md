# Notification sound

`raw/sonar_notification.wav` is the shared incoming-message sound for the
native Apple, Android, and Compose Desktop apps. It is a mono, 44.1 kHz,
16-bit PCM conversion of the user-supplied source audio.
